@model Manage_tasks_Biznes_Logic.Model.TasksList


<div class="container text-center">
    <div class="row align-items-start">
        <div class="col">
            @await Component.InvokeAsync("TasksRow", new {list = Model,statusId = 0})

            @await Component.InvokeAsync("NewTask")
        </div>
        <div class="col">
            @await Component.InvokeAsync("TasksRow", new {list = Model,statusId = 1})
        </div>
        <div class="col">
            @await Component.InvokeAsync("TasksRow", new {list = Model,statusId = 2})
        </div>
    </div>
</div>
